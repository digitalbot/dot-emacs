;;;----------------------------------------
;;; setup
;;;----------------------------------------


;; minibuffer
(setq history-length 3500)
(savehist-mode t)

;; no dialog-box use minibuffer
(setq use-dialog-box nil)

(icomplete-mode 1)
